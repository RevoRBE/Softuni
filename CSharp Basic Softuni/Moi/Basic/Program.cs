using System;
using System.Linq;

class FieldPlay
{
    static void Main()
    {
        int fieldSize = int.Parse(Console.ReadLine());
        var bugsI = Console.ReadLine().Split().Select(int.Parse).Where(a => a>=0 && a< fieldSize).ToArray();
        var bugs = new int[fieldSize];
        foreach (var bugI in collection)
        {
            
        }
        while (commands[0] )
        {
            var line = Console.ReadLine();
            if (line == "end")
            {
                break;
            }
            var tokens = line.Split();
            var lad
        }
    }
}